const fs = require("fs");
const path = require("path");
const chalk = require("chalk");
const { prompt, Prompt } = require("enquirer");
const enquirer = require("enquirer");
module.id = "moduleImports";
module.exports = { fs, path, chalk, prompt };
